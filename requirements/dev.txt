# ─────────────────────────────────────────────────────────────────────────────
# ComplianceLoop — Development / test requirements
# Install: pip install -r requirements/base.txt -r requirements/dev.txt
# Do NOT install in production Docker images
# ─────────────────────────────────────────────────────────────────────────────

-r base.txt

# Testing framework
pytest==8.3.2
pytest-asyncio==0.23.8           # Async test support
pytest-cov==5.0.0                # Coverage reporting
pytest-mock==3.14.0              # Mock fixtures
pytest-xdist==3.6.1              # Parallel test execution
pytest-timeout==2.3.1            # Test timeout enforcement
pytest-httpx==0.30.0             # Mock httpx calls in tests

# TestContainers (spin up real Postgres, Redis, MinIO for integration tests)
testcontainers==4.7.2
testcontainers[postgres]==4.7.2
testcontainers[redis]==4.7.2
testcontainers[minio]==4.7.2

# FastAPI test client
httpx==0.27.0                    # Async test client for FastAPI
asgi-lifespan==2.1.0             # Manage FastAPI lifespan in tests

# Factories / fixtures
factory-boy==3.3.1               # Model factories for test data generation
faker==26.3.0                    # Fake data generation (names, addresses, etc.)
Faker-India==1.0.1               # Indian-specific fake data (PAN, Aadhaar-like)

# Code quality
black==24.8.0
ruff==0.5.7
mypy==1.11.1
types-python-jose==3.3.4.20240106
types-passlib==1.7.7.20240327
types-redis==4.6.0.20241004
types-beautifulsoup4==4.12.0.20240511
types-python-dateutil==2.9.0.20240316
types-pytz==2024.1.0.20240417
types-regex==2024.7.24.20240726

# Debugging
ipython==8.26.0
ipdb==0.13.13
rich==13.7.1                     # Pretty printing for debug output

# Documentation
mkdocs==1.6.0                    # Docs site generation (optional)
mkdocs-material==9.5.32

# Pre-commit hooks
pre-commit==3.8.0